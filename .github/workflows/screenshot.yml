name: Site Screenshots

on:
  schedule:
    - cron: '40 5 * * 0'   # Weekly, after the stopped-service recheck
  workflow_dispatch:

permissions:
  contents: write

jobs:
  screenshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Chinese fonts
        run: sudo apt-get install -y fonts-noto-cjk

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install playwright
          playwright install chromium
          playwright install-deps chromium

      - name: Capture new or changed sites only
        run: python scripts/screenshot.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/screenshots/ data/screenshot_hashes.csv data/screenshot_failures.csv
          git diff --staged --quiet || (git commit -m "screenshots: $(date -u '+%Y-%m-%d %H:%M') UTC" && git pull --rebase origin main && git push)
