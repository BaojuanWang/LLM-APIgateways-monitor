𝓤𝓸𝓒𝓸𝓭𝓮 You need to enable JavaScript to run this app. // Preload custom homepage content before React mounts, so the shell // can hide the built-in navigation/footer on first paint. (function () { try { var xhr = new XMLHttpRequest(); xhr.open('GET', '/api/home_page_content', false); xhr.send(); if (xhr.status === 200) { var res = JSON.parse(xhr.responseText); if (res && res.success) { var content = res.data || ''; window.__HOME_PAGE_CONTENT__ = content; try { if (content) { localStorage.setItem('home_page_content_raw', content); } else { localStorage.removeItem('home_page_content_raw'); } } catch (e) {} } } } catch (e) {} })();