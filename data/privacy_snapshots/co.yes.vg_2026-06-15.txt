Loading... // Dynamically load branding from admin settings (before React boot) (function () { fetch('/api/v1/public/admin-settings') .then((res) => res.json()) .then((data) => { const siteName = (data && String(data.site_name || '').trim()) || 'API Router'; document.title = siteName; const metaDesc = document.querySelector('meta[name="description"]'); if (metaDesc) { metaDesc.setAttribute('content', siteName + ' - API routing with billing and user management'); } const iconUrl = (data && String(data.favicon_url || data.logo_url || '').trim()) || ''; if (iconUrl) { const favicon = document.querySelector('link[rel="icon"]'); if (favicon) favicon.href = iconUrl; const appleTouchIcon = document.querySelector('link[rel="apple-touch-icon"]'); if (appleTouchIcon) appleTouchIcon.href = iconUrl; } // Update manifest name/icons dynamically (for PWA installs) const manifestLink = document.querySelector('link[rel="manifest"]'); if (manifestLink) { const manifestHref = manifestLink.getAttribute('href') || '/manifest.json'; fetch(manifestHref) .then((r) => r.json()) .then((manifest) => { if (!manifest || typeof manifest !== 'object') return; manifest.name = siteName; manifest.short_name = siteName; if (iconUrl) { const lower = iconUrl.toLowerCase(); const type = lower.endsWith('.svg') ? 'image/svg+xml' : lower.endsWith('.ico') ? 'image/x-icon' : 'image/png'; const existing = Array.isArray(manifest.icons) ? manifest.icons : []; const hasIcon = existing.some((icon) => icon && icon.src === iconUrl); if (!hasIcon) { existing.unshift({ src: iconUrl, sizes: '512x512', type }); } manifest.icons = existing; } const blob = new Blob([JSON.stringify(manifest)], { type: 'application/manifest+json' }); manifestLink.setAttribute('href', URL.createObjectURL(blob)); }) .catch(() => {}); } }) .catch(() => { document.title = 'API Router'; }); })(); /* Prevent white flash when switching tabs */ html, body { background-color: #000000; /* match dark default to prevent flash */ color: #ffffff; color-scheme: dark; font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; margin: 0; padding: 0; transition: none; /* Disable any transitions that might cause flash */ } /* Loading state styling */ #root { min-height: 100vh; background-color: transparent; transition: none; /* Disable transitions */ } /* Prevent any flash during navigation */ * { box-sizing: border-box; } /* Optional: Add a subtle loading animation */ .loading-splash { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: #000000; display: flex; align-items: center; justify-content: center; z-index: 9999; } .loading-spinner { width: 32px; height: 32px; border: 2px solid rgba(255, 255, 255, 0.12); border-top: 2px solid #0099ff; border-radius: 50%; animation: spin 1s linear infinite; opacity: 0.7; } @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } } /* Prevent flash of unstyled content */ .react-loading { opacity: 0; transition: opacity 0.2s ease-in-out; } .react-loaded { opacity: 1; } You need to enable JavaScript to run this app.