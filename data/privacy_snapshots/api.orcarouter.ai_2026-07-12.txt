OrcaRouter — One API. Every model. Zero middleman. , BEFORE the __skipAnalytics guard script below runs, so on a cold-load of /redeem/:code the stylesheet fetch would otherwise send the bearer voucher URL to fonts.googleapis.com as the Referer. Strip it — Google Fonts needs no referrer — closing the same leak the JS analytics guard closes for GTM/Meta/Crisp (codex round-21 P2). --> window.addEventListener('beforeinstallprompt', function (e) { try { if (!window.matchMedia || !window.matchMedia('(max-width: 767px)').matches) { return; } } catch (_) { return; } e.preventDefault(); window.__deferredInstallPrompt = e; }); (function () { var p = window.location.pathname || ''; // The public /redeem/:code claim route carries a redeemable bearer // voucher code in the path. Skip third-party analytics so GTM/Meta Pixel // never capture the code via page_location/referrer. Matched with an // optional leading locale segment (/zh-CN/redeem/:code etc., served by // the /:loc/* SPA fallback) and requires /redeem to be a full path // segment so /redeemed... is not caught. var isRedeem = /^(\/[A-Za-z]{2}(-[A-Za-z0-9]+)?)?\/redeem(\/|$)/.test(p); // A signed-out claim stashes its /redeem/:code return target in // localStorage['post_login_redirect'] (never a URL) and sends the visitor // to /login. That value carries the bearer voucher code and is readable by // any first-party analytics tag, so suppress analytics on ANY page load // while a pending redeem return target exists — closing the /login // cold-load / OAuth-callback window until the code is consumed and the key // cleared (helpers/auth.jsx consumePostLoginRedirect removes it). // Check the breadcrumbs a claim writes: the localStorage return target, // the signed-out sessionStorage pending code, AND the logged-in card-gate // pending_redeem_claim (stashed before the $0 Stripe card setup so the // return can finish the claim — an abandoned Checkout can leave it while // the visitor lands on /console). If localStorage is unavailable / its // write failed, the raw code can still sit in one of the sessionStorage // keys — same-origin analytics could read it — so any breadcrumb suppresses // analytics. var pendingRedeem = false; try { pendingRedeem = /(^|\/)redeem\//.test( localStorage.getItem('post_login_redirect') || '', ); } catch (e) { /* localStorage unavailable — ignore */ } try { pendingRedeem = pendingRedeem || !!(sessionStorage.getItem('pending_redeem_code') || '') || !!(sessionStorage.getItem('pending_redeem_claim') || ''); } catch (e) { /* sessionStorage unavailable — ignore */ } window.__skipAnalytics = p.indexOf('/user/reset') === 0 || p.indexOf('/impersonate') === 0 || p.indexOf('/login/magic') === 0 || isRedeem || pendingRedeem; })(); if(!window.__skipAnalytics){(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start': new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0], j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src= 'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f); })(window,document,'script','dataLayer','GTM-T674D3WQ');} (function () { // Skip Meta Pixel on sensitive paths (password-reset tokens, // admin View-Account bootstrap code) so fbevents.js never sends // that URL / referrer to a third-party analytics sink. The shared // guard above also already scrubbed /impersonate's query string. if (window.__skipAnalytics) { return; } !function(f,b,e,v,n,t,s) {if(f.fbq)return;n=f.fbq=function(){n.callMethod? n.callMethod.apply(n,arguments):n.queue.push(arguments)}; if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0'; n.queue=[];t=b.createElement(e);t.async=!0; t.src=v;s=b.getElementsByTagName(e)[0]; s.parentNode.insertBefore(t,s)}(window, document,'script', 'https://connect.facebook.net/en_US/fbevents.js'); fbq('init', '656083250933792'); fbq('track', 'PageView'); })(); You need to enable JavaScript to run this app.