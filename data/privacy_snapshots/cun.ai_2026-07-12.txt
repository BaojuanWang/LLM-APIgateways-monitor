New API window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'G-DDJY6YW5RC'); var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date(); // Lift the bubble ~100px off the bottom so it clears pagination buttons. var TAWK_BOTTOM_OFFSET = 100; var TAWK_RIGHT_OFFSET = 20; Tawk_API.customStyle = { visibility: { desktop: { position: 'br', xOffset: TAWK_RIGHT_OFFSET, yOffset: TAWK_BOTTOM_OFFSET }, mobile: { position: 'br', xOffset: 10, yOffset: 80 } } }; // Remembered widget position (viewport coords of the top-left corner). var TAWK_POS_KEY = 'tawk_widget_pos_v1'; // Movement (px) before a press is treated as a drag rather than a tap. var TAWK_DRAG_THRESHOLD = 6; // Max bubble size; anything larger means the chat window is open, so we // release the overlay and let the iframe handle interaction. var TAWK_BUBBLE_MAX = 160; Tawk_API.onLoad = function(){ if (window.innerWidth , so pointer events over // it never reach the parent document. We overlay a transparent capture // layer on top of the bubble: a tap toggles the chat, a drag repositions // the widget, and the position is persisted so it stops blocking page // controls (e.g. pagination) permanently instead of just for one visit. (function setupDrag(retries) { retries = retries || 0; // The visible bubble is the SMALLEST currently-visible tawk iframe. // (tawk also injects a larger, usually-hidden chat-window iframe.) function findBubbleIframe() { var frames = document.querySelectorAll( 'iframe[src*="tawk.to"], iframe[title="chat widget"], iframe[title*="chat" i]' ); var best = null, bestArea = Infinity; for (var i = 0; i TAWK_BUBBLE_MAX || r.height > TAWK_BUBBLE_MAX) continue; var area = r.width * r.height; if (area TAWK_BUBBLE_MAX || r.height > TAWK_BUBBLE_MAX) return null; return r; } function syncOverlay() { var r = bubbleRect(); if (!r) { overlay.style.display = 'none'; return; } overlay.style.display = 'block'; overlay.style.left = r.left + 'px'; overlay.style.top = r.top + 'px'; overlay.style.width = r.width + 'px'; overlay.style.height = r.height + 'px'; } function moveTo(left, top) { var mr = movable.getBoundingClientRect(); var w = mr.width || 80, h = mr.height || 80; var nx = Math.max(0, Math.min(window.innerWidth - w, left)); var ny = Math.max(0, Math.min(window.innerHeight - h, top)); movable.style.setProperty('position', 'fixed', 'important'); movable.style.setProperty('left', nx + 'px', 'important'); movable.style.setProperty('top', ny + 'px', 'important'); movable.style.setProperty('right', 'auto', 'important'); movable.style.setProperty('bottom', 'auto', 'important'); movable.style.setProperty('margin', '0', 'important'); syncOverlay(); } function applySavedPosition() { try { var saved = JSON.parse(localStorage.getItem(TAWK_POS_KEY) || 'null'); if (saved && typeof saved.left === 'number' && typeof saved.top === 'number') { moveTo(saved.left, saved.top); return; } } catch (e) {} // No saved position yet: default to the bottom-right corner but // lifted ~100px off the bottom so it clears pagination controls. var mr = movable.getBoundingClientRect(); var w = mr.width || 80, h = mr.height || 80; moveTo(window.innerWidth - w - TAWK_RIGHT_OFFSET, window.innerHeight - h - TAWK_BOTTOM_OFFSET); } var dragging = false, moved = false, sx, sy, ox, oy; function onStart(e) { if (!bubbleRect()) return; var ev = e.touches ? e.touches[0] : e; dragging = true; moved = false; sx = ev.clientX; sy = ev.clientY; var mr = movable.getBoundingClientRect(); ox = mr.left; oy = mr.top; overlay.style.cursor = 'grabbing'; e.preventDefault(); } function onMove(e) { if (!dragging) return; var ev = e.touches ? e.touches[0] : e; var dx = ev.clientX - sx, dy = ev.clientY - sy; if (!moved && Math.abs(dx) + Math.abs(dy)