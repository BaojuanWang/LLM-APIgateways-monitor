(function () { var mode = 'production'; var host = window.location.hostname; var isLocalAddress = host === 'localhost' || host === '127.0.0.1' || host === '0.0.0.0' || host === '::1' || /^192\.168\./.test(host) || /^10\./.test(host) || /^172\.(1[6-9]|2\d|3[0-1])\./.test(host); if (mode !== 'production' || isLocalAddress) { return; } (function (w, d, s, l, i) { w[l] = w[l] || []; w[l].push({ 'gtm.start': new Date().getTime(), event: 'gtm.js' }); var f = d.getElementsByTagName(s)[0], j = d.createElement(s), dl = l != 'dataLayer' ? '&l=' + l : ''; j.async = true; j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl; f.parentNode.insertBefore(j, f); })(window, document, 'script', 'dataLayer', 'GTM-WPKVJDM9'); })(); APINebula (function () { try { var SITE_ID = '108012'; var USER_KEY = 'user'; function readUserId() { try { var raw = localStorage.getItem(USER_KEY); if (!raw) return null; var u = JSON.parse(raw); if (u && u.id !== undefined && u.id !== null) return String(u.id); return null; } catch (e) { return null; } } var pending; // undefined=无变化, null=登出, string=登录 function flush() { try { if (!window.trackly) return false; if (pending === undefined) return true; try { if (pending === null) { window.trackly.clearUserId && window.trackly.clearUserId(); } else { window.trackly.setUserId && window.trackly.setUserId(pending); } } catch (e) {} pending = undefined; return true; } catch (e) { return true; // 出错也算"已处理"，避免轮询死循环 } } function apply(id) { try { pending = id; if (flush()) return; var t = setInterval(function () { try { if (flush()) clearInterval(t); } catch (e) { clearInterval(t); } }, 200); setTimeout(function () { try { clearInterval(t); } catch (e) {} }, 30000); } catch (e) {} } // 1) 首次加载时读取一次 apply(readUserId()); // 2) Hook 当前 tab 的 localStorage 写入（storage 事件不会在写入方触发） // 原始 setItem/removeItem 先于我们的逻辑执行，任何异常都不会回传给调用方 try { var origSet = Storage.prototype.setItem; var origRemove = Storage.prototype.removeItem; Storage.prototype.setItem = function (k, v) { var r = origSet.apply(this, arguments); try { if (this === window.localStorage && k === USER_KEY) apply(readUserId()); } catch (e) {} return r; }; Storage.prototype.removeItem = function (k) { var r = origRemove.apply(this, arguments); try { if (this === window.localStorage && k === USER_KEY) apply(null); } catch (e) {} return r; }; } catch (e) {} // 3) 跨 tab 登录/登出同步 try { window.addEventListener('storage', function (e) { try { if (e.key === USER_KEY) apply(readUserId()); } catch (err) {} }); } catch (e) {} // 4) 加载 base.js（onerror 静默失败） try { var s = document.createElement('script'); s.async = true; s.src = 'https://track.risej.com/record/base.js?id=' + SITE_ID; s.onload = function () { try { flush(); } catch (e) {} }; s.onerror = function () {}; (document.head || document.documentElement).appendChild(s); } catch (e) {} } catch (e) { // 整段失败也不冒泡 } })(); You need to enable JavaScript to run this app.